[project]
name = "subset-sum-gpu"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "numpy>=2.1",
    "ortools>=9.14",
]

[dependency-groups]
dev = ["jupyterlab>=4"]

[tool.uv]
package = false
